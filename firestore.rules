rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Permitir acceso completo a todas las colecciones (para desarrollo/testing)
    // IMPORTANTE: En producción, deberías restringir estos permisos
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
